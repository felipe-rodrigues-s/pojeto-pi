const array = [].concat({
    nome: "Felipe",
    descriptor: ["moreno", "brasileiro", "estudioso"]
  });
  


const ob = array.concat({
    nome: "test"
})

console.log(ob);


carts = {
    xx,
    xx,
    xx,
    yy,
    zz,
    mm,
}

percorerr carts -> 
se  carts[li].id == carts[i+1]
    array = [xx, xx,xx]
    array = [yy]
    array = [zz]
    array = [mm]

    carts [0] = [xx, xx,xx]
    carts [1] yy
